[project]
name = "guagua-sim"
version = "0.1.0"
description = "呱呱仿真/训练子项目 (uv 管理) - MuJoCo + W&B 实验追踪"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mujoco>=3.8.0",
    "wandb>=0.26.1",
]
