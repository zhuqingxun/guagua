bash -c 'set +e;D=/home/raspios/open_duck_mini_ws/mcp-openduck;cd $D || { echo "[ERR] cd $D failed";exit 1; };echo "[A pwd+owner]";pwd;ls -ld $D;echo "[B git remote]";git remote -v;echo "[C connectivity tests]";for h in github.com gitee.com codeload.github.com; do printf "%-22s " "$h";curl -sS --max-time 5 -o /dev/null -w "http=%{http_code} t=%{time_total}s\n" https://$h/ 2>&1 || echo fail; done;echo "[D git status]";git status --short;echo "[E git log -1]";git log -1 --oneline;echo "[F git fetch]";git fetch --all 2>&1 | head -10;echo "[G ahead/behind]";git rev-list --left-right --count HEAD...@{u} 2>&1;echo "[H pull decision]";DIRTY=$(git status --porcelain | wc -l);BEHIND=$(git rev-list --count HEAD..@{u} 2>/dev/null || echo "?");if [ "$DIRTY" = "0" ] && [ "$BEHIND" != "0" ] && [ "$BEHIND" != "?" ]; then echo "clean+behind=$BEHIND, pulling";git pull --ff-only 2>&1 | head -20; else echo "skip pull (dirty=$DIRTY behind=$BEHIND)"; fi;echo "[I requirements.txt]";cat requirements.txt 2>&1;echo "[J pip env]";which python;which pip;python --version;echo "VIRTUAL_ENV=$VIRTUAL_ENV";echo "[K pip install]";pip install -r requirements.txt 2>&1 | tail -20;echo "[L key scripts]";ls -la start_mcp.sh start_duck_mcp.sh mcp_point_example.sh mcp_point.sh openduck.py 2>&1;echo "[M done]"'
