---
description: "[handoff] Runtime 仓库 vendor-baseline 化 + PS4 patch 完成, systemd unit 草拟好, 鸭子 11:30 断电待续"
status: archived
created_at: 2026-05-23T11:35:00
updated_at: 2026-05-23T14:00:00
archived_at: 2026-05-23T14:00:00
consumed_at: 2026-05-23T14:00:00
---

# Handoff 2026-05-23 v1: vendor-baseline 化完成 + systemd 待 deploy + 鸭子断电

## ⭐ 下次会话第一步(复制粘贴)

```
cd D:/CODE/guagua
读 rpiv/handoff-2026-05-23-v1.md
读 docs/duck-workspace-state.md  # 任务级 source of truth (三 sub-repos manifest)
读 docs/s0-handoff-passed-2026-05-22.md  # 上一份 handoff (5/22 S0 通关)
读 docs/m3-s0-checklist.md  # M3-W2 阶段总进度

调用 /rpiv-loop:handoff --mark-consumed rpiv/handoff-2026-05-23-v1.md

按本 handoff "下一步动作" 段:
1. ssh duck 'cd ~/open_duck_mini_ws/Open_Duck_Mini_Runtime && git fsck --full && git log --oneline --all | head -5 && git status --short' 验证 SD 卡完整 + 三个 commit 在
2. fsck 通过后, scp D:/CODE/guagua/docs/systemd/*.service duck:/tmp/ + ssh duck 'sudo cp /tmp/*.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable mcp-openduck duck-mcp-runtime' (enable 但不 start)
3. 与用户确认鸭子物理位置+电机电源安全后, sudo systemctl start mcp-openduck && sleep 8 && sudo systemctl start duck-mcp-runtime → 喊话验收 → rotate xiaozhi token → sudo reboot 终验 (S0 #10)
```

## TL;DR

- ✅ Runtime 仓库 baseline 化完成: `vendor-baseline-2026-05-23` (`0671b96` 81 files) + `guagua-customizations` (HEAD `6f93be8`) 两分支, working tree 干净
- ✅ PS4 patch 写入 git history: `v2_rl_walk_mujoco_mcp.py:125` try/except 包 `PS4Controller(self.command_freq)`, 失败 fallback `_StubPS4` (只实现 `get_last_command()` 返回 `[0.0]*7`). 解除 systemd 自启对蓝牙手柄的硬依赖.
- ✅ PC 端 manifest 完整: `docs/duck-workspace-state.md` (sub-repos 索引 + 灾难恢复 SOP) + `docs/patches/ps4-controller-fallback-2026-05-23.diff` + `docs/systemd/{mcp-openduck,duck-mcp-runtime}.service` (草稿)
- ⛔ 11:30 鸭子硬件断电. `sudo poweroff` 已 trigger (ssh remote closed = sshd 关闭, 确认命令已执行), 但用户多久后硬断电不确定, SD 卡有**小概率** ext4 journal 损坏
- ⏸️ systemd unit deploy 推迟到下次上电; xiaozhi token rotate 仍 pending (5/22 起的已知泄漏 token, 本会话又印过一次同 token); S0 #10 reboot 终验待做

## 下一步动作(新会话主动执行)

1. **SD 卡完整性验证**(每次硬断电后都要做):
   ```bash
   ssh duck 'cd /home/raspios/open_duck_mini_ws/Open_Duck_Mini_Runtime && git fsck --full && git log --oneline --all | head -5 && git status --short'
   ```
   期望: `git fsck` 无错; `git log` 看到 `6f93be8 patch: PS4Controller` + `9ad727e chore: gitignore` + `0671b96 snapshot: 爱折腾出厂态` 三 commit; `git status` 空 (HEAD 仍在 `guagua-customizations`).
   **任一不符** → 不要继续, 进 Plan B (从 `.git/objects` 重建, 或从 baseline 重做 patch, 或 SD 卡重建)

2. **Systemd unit deploy(enable, 不 start)**:
   ```bash
   scp D:/CODE/guagua/docs/systemd/mcp-openduck.service duck:/tmp/
   scp D:/CODE/guagua/docs/systemd/duck-mcp-runtime.service duck:/tmp/
   ssh duck 'sudo cp /tmp/mcp-openduck.service /tmp/duck-mcp-runtime.service /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable mcp-openduck duck-mcp-runtime && systemctl list-unit-files | grep -E "mcp-openduck|duck-mcp-runtime"'
   ```
   期望: `enabled` 状态

3. **物理安全确认 → start → 喊话验收 → token rotate → reboot 终验**:
   ⚠️ 跑 `duck-mcp-runtime` = 电机进入 init 姿势, 鸭子可能突然动. 必须先确认: ① 鸭子物理位置安全 (地面/平台,无障碍) ② 电机电源开关状态 ③ 用户在场可拉电.
   ```bash
   ssh duck 'sudo systemctl start mcp-openduck && sleep 8 && sudo systemctl start duck-mcp-runtime && sleep 5 && sudo systemctl status mcp-openduck duck-mcp-runtime --no-pager | head -30 && ss -tnp 2>/dev/null | grep -E ":443|:6789" | head'
   ```
   期望: 两个 unit `active (running)`; ss 看到本地 :6789 listen + outbound :443 (wss 到 xiaozhi.me).
   通过后:
   - 用户对小智 ESP32 喊话验收 (例: "向前走" / "转一圈")
   - **rotate xiaozhi token**(已知泄漏, 必做): xiaozhi.me web 解绑+重绑 ESP32 → 拿新 wss URL → ssh duck 改 `/home/raspios/open_duck_mini_ws/mcp-openduck/mcp_point.sh` → restart mcp-openduck
   - `ssh duck sudo reboot` → 等 2 分钟 → 重新验证 2 个 unit active + 喊话再验一次 = **S0 #10 终验通过**

## 关键 ID 速查(本会话)

| ID / 名 | 内容 | 创建时间 |
|---|---|---|
| `0671b9686e4e6dc49818c742d75681975e51e029` (短 `0671b96`) | Runtime `vendor-baseline-2026-05-23` 分支 HEAD = 爱折腾出厂态完整 snapshot (81 files / +41291/-126) | 2026-05-23 ~11:21 |
| `9ad727e6d0777e5ef73f3be9e979ff75d65f56aa` (短 `9ad727e`) | `guagua-customizations` 第一个 commit: chore: gitignore + rm cached log/jpg | 2026-05-23 ~11:21 |
| `6f93be89f4c965954a12673da1e620ca20218958` (短 `6f93be8`) | `guagua-customizations` 第二个 commit (HEAD): patch: PS4Controller fallback to stub | 2026-05-23 ~11:21 |
| `34c60ef` | Runtime upstream `v2` 分支 HEAD (apirrone "antennas test"), baseline 的 parent | 2025-09-28 |
| 鸭子 SSH 别名 | `ssh duck` → `raspios@192.168.3.166` (PC `~/.ssh/config` 已配, 5/19 done) | — |
| 鸭子 hostname | `OPENDUCKMINI-V108` (爱折腾整机版 Pi 3B+) | — |
| xiaozhi.me 智能体 | `agentId=1886229 / userId=939129 / endpointId=agent_1886229` (音色"湾湾小何", 模型"小智 Lite") | 5/22 |
| 已泄漏 wss token (待 rotate) | `wss://api.xiaozhi.me/mcp/?token=eyJhbGc...` (5/22 + 5/23 进 transcript 两次) | exp=1811017537 (2027) |

## 关键文件清单(PC + 鸭子 两端)

### PC 端 (D:/CODE/guagua/)

| 文件 | 用途 |
|---|---|
| `docs/duck-workspace-state.md` | ★ source of truth: 三 sub-repos 索引 + 分支 + patch 列表 + 灾难恢复 SOP |
| `docs/patches/ps4-controller-fallback-2026-05-23.diff` | PS4 patch 的 unified diff 副本 (将来 upstream pull 后能 re-apply) |
| `docs/systemd/mcp-openduck.service` | systemd unit 草稿 (start_mcp.sh wrapper) |
| `docs/systemd/duck-mcp-runtime.service` | systemd unit 草稿 (start_duck_mcp.sh wrapper + ExecStartPre 等 :6789 listen) |
| `docs/s0-handoff-passed-2026-05-22.md` | 5/22 S0 通关 handoff (含坑 1 PS4 + 坑 2 token 泄漏) |
| `docs/m3-s0-checklist.md` | M3-W2 阶段总进度 (Step 1-3 已 done, Step 4 录 demo+备份 SD 待做) |
| `rpiv/todo/m3-issue-pigeon-toed.md` | 内八字问题, open / medium / 暂缓 |

### 鸭子端 (`raspios@192.168.3.166`)

| 路径 | 状态 |
|---|---|
| `~/open_duck_mini_ws/Open_Duck_Mini_Runtime/` | git: `vendor-baseline-2026-05-23` + `guagua-customizations` 两分支, HEAD = `guagua-customizations` |
| `~/open_duck_mini_ws/mcp-openduck/` | git: master HEAD `3fa9fd5`, clean (mcp_point.sh 被 .gitignore 排除, 含 wss token) |
| `~/open_duck_mini_ws/Open_Duck_Mini/` | git: v2 HEAD `cc4a2d4`, clean (含 BEST_WALK_ONNX_2.onnx) |
| `~/venv_duck/` | Python 3.11.2 venv, mcp-openduck + Runtime 都用它 |
| `~/.ssh/id_ed25519_gitee` + pub | 5/23 生成的鸭子出向 ed25519 (deferred GitHub remote 时用) |
| `~/.ssh/config` | 含 `Host gitee.com IdentityFile ~/.ssh/id_ed25519_gitee` (deferred GitHub 时改 hostname) |
| `/etc/systemd/system/mcp-openduck.service` | **不存在** (本会话设计完没 deploy, 等下次会话 scp + cp + enable) |
| `/etc/systemd/system/duck-mcp-runtime.service` | **不存在** (同上) |
| `/home/raspios/open_duck_mini_ws/mcp-openduck/mcp_point.sh` | 含 5/22 起的 wss URL token (343 字节, 在 .gitignore 中不进 git) |

## 教训锚点

- **鸭子电量监控缺位**: 本会话 11:30 鸭子硬件断电时, 我没有任何主动机制感知"电量低"。整个会话期间没查过 `/sys/class/power_supply/` 或 `vcgencmd get_throttled`, 没有低电量预警。如果有过监控, `sudo poweroff` 可以提早 5-10 分钟优雅触发, 避免硬断电的 SD 卡损坏风险.
  - **沉淀方向**: 写一个 `rpiv/todo/duck-low-battery-monitor.md`, 描述需求 = 周期检查鸭子电池电压 / throttled 标志 / 离线时长, 低于阈值时主动告警用户. 跟现有 `docs/m3-s0-checklist.md` Step 4 备份 SD 卡互补 (备份是事后, 监控是事前).
  - **跨项目复用价值**: 任何 Pi / 嵌入式设备远程调试都适用. 考虑写入 `~/.claude/rules/raspberry-pi-debugging.md` (已存在该规则文件, 第 7 节"标准开场仪式"可加一条电量探针).
- **`sudo poweroff` 触发后 ssh 立即 disconnect 是设计行为**: 不要凭 "ssh remote closed" 判断"已关机", 真正的 halt 还要 5-10 秒走 sync/unmount. 用户硬断电时序仍需提示 (本会话 11:30 已发生, 没造成损坏需 fsck verify).

## 元规则(新会话务必遵守)

- **CLAUDE.md "成熟软件架构, 修改前确认 + 留历史"**: 本会话第二轮才 git 化 (前面直接 patch + .bak 兜底, 用户后来明确要 git). 下次会话动鸭子 Runtime 仓库代码前, **必须** verify 在 `guagua-customizations` 分支 + 改完后 commit, 不能再走"直接改文件 + .bak 兜底"路径
- **S0 红线仍生效**: 不替换爱折腾自家 MCP (S1 才换); 不改 50Hz 控制循环; 不动 ONNX 权重; 不改 ESP32 配网到 5G `1101_5G`
- **GitHub remote backup deferred**: 用户明确不走 Gitee, push 走 GitHub, 暂时只本地 commit. Task #10 是 deferred todo, 不在本会话/下次会话主路径
- **token 泄漏不要再扩大**: 任何打印 `mcp_point.sh` / `cat` 该文件的命令必须 sed 替换 token 段
